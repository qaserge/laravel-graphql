type User {
  id: ID!
  name: String!
  email: String!
}

# example of use: 
# {
#   users (first:2, page:1) {
#     data {
#       name
#       id
#       email
#     }
#     paginatorInfo {
#       currentPage
#       lastPage
#       hasMorePages
#       lastItem
#       total
#     }
#   }
# }
type Query {
  users: [User!] @paginate
  user(id: ID! @eq) : User @find #filter by user id
}