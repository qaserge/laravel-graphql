type User {
  id: ID!
  name: String!
  email: String!
}

# example of use: 
# user(id:2) {
#  name
# }
"""
type Query {
  users: [User!] @all
}
"""

# example of use: 
# user(id:2) {
#  name
# }
type Query {
  users: [User!] @all
  user(id: ID! @eq) : User @find #filter by user id
}

